<package>
	<description brief="tod_detecting">

		tod_detecting

  </description>
	<author>Alexander Shishkov, Ethan Rublee, Vincent Rabaud</author>
	<license>BSD</license>
	<review status="unreviewed" notes="" />
	<url>http://ros.org/wiki/tod_detecting</url>

	<!-- eigen -->
	<depend package="eigen" />
	
	<!-- opencv -->
	<depend package="opencv2" />
	
	<!-- tod_* -->
	<depend package="tod_core" />
	<depend package="tod_training" />
	
	<!-- vslam -->
	<depend package="posest" />
	
	<!-- PCL depends -->
	<depend package="pcl" />
	<depend package="pcl_visualization" />
	
	<!-- rosbag -->
	<depend package="rosbag" />

	<export>
		<cpp cflags="-I${prefix}/include"
			lflags="-Wl,-rpath,${prefix}/lib -L${prefix}/lib -ltod_detecting" />
	</export>

</package>


