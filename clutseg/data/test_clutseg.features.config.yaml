%YAML:1.0
# FeatureExtractionParams
feature_extraction_params:
   detector_type: FAST
   extractor_type: multi-scale
   descriptor_type: rBRIEF
   detector_params:
      max_features: 0.
      min_features: 0.
      threshold: 30.
   extractor_params:
      octaves: 3.
